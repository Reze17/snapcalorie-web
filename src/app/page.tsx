export default function Home() {
  return (
    <main className="mx-auto flex min-h-screen w-full max-w-screen-sm flex-col items-center justify-center gap-2 px-4 py-8 text-center">
      <h1 className="text-2xl font-semibold">SnapCalorie Web</h1>
      <p className="text-sm text-[var(--foreground)]/70">
        Scaffold running. Phase 0 of 10.
      </p>
    </main>
  );
}
